# FLUVORA environment configuration (copy to server/.env)

# API port
PORT=5000

# Optional — leave empty to run on the built-in in-memory prototype store.
# Provide a MongoDB connection string to persist roads & reports across restarts.
MONGODB_URI=

# Optional — real weather integration for production.
# The prototype simulates rainfall from the Command Center instead.
WEATHER_API_KEY=
